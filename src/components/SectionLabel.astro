---
interface Props {
  text: string;
  ornament?: string;
}

const { text, ornament = '✦' } = Astro.props;
---

<div class="section-label">
  <span class="section-label-text">{text}</span>
  <div class="section-rule"></div>
  <span class="section-ornament">{ornament}</span>
</div>
